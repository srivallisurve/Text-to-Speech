# Start from a clean, lightweight Python image
FROM python:3.10-slim

# Set the working directory inside the container
WORKDIR /app

# Copy the requirements file and install dependencies (now much faster!)
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy the rest of your application code (app.py)
COPY . .

# Expose the port that Gradio runs on (default is 7860)
EXPOSE 7860

# Define the command to run the application using the absolute path
CMD ["python", "/app/app.py"]
